tags:
  # Снимки экранов. Единственный способ увидеть интерфейс на этой машине:
  # Android SDK локально нет, десктопной цели у проекта тоже, так что
  # golden-рендер — не проверка на регресс, а инструмент разработки.
  # Тег объявлен, чтобы его можно было исключить одной командой:
  #   flutter test --exclude-tags golden
  golden:
    description: Снимки экранов через matchesGoldenFile
